import { Text } from 'react-native';
import ScreenShell from '@/components/ScreenShell';

export default function EarningsScreen() {
  return (
    <ScreenShell title="Earnings" subtitle="Track gross, expenses and net profit">
      <Text>Earnings dashboard exists under src/features/earnings/.</Text>
    </ScreenShell>
  );
}
