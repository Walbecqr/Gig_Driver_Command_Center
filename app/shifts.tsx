import { Text } from 'react-native';
import ScreenShell from '@/components/ScreenShell';

export default function ShiftsScreen() {
  return (
    <ScreenShell title="Shift Tracker" subtitle="Track start/end and mileage in progress">
      <Text>Shift tracking module is scaffolded in src/features/shifts/.</Text>
    </ScreenShell>
  );
}
