import { Text } from 'react-native';
import ScreenShell from '@/components/ScreenShell';

export default function MarketScreen() {
  return (
    <ScreenShell title="Market Intel" subtitle="Demand heatmaps, recommendations, relocation hints">
      <Text>Market intelligence module is in src/features/market/.</Text>
    </ScreenShell>
  );
}
