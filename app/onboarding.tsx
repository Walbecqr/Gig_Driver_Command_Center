import { Text } from 'react-native';
import ScreenShell from '@/components/ScreenShell';

export default function OnboardingScreen() {
  return (
    <ScreenShell title="Onboarding" subtitle="Set up account and delivery platforms">
      <Text>Onboarding flow skeleton registered. Add forms in src/features/onboarding/.</Text>
    </ScreenShell>
  );
}
